name: Build and Push Docker Image

on:
  push:
    branches: [main]
   
  pull_request:
    branches: [main]
  

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: mohamedamineaitjaakike
          password: dckr_pat_wW-SC1U9HQbMYZ4Y3t443pflKoM

      - name: Build and push Docker image
        env:
          REPOSITORY: mohamedamineaitjaakike/php_laravel_angular
          IMAGE_TAG: latest
        run: |
          docker build -t mohamedamineaitjaakike/php_laravel_angular:latest ./configuration
          docker push  mohamedamineaitjaakike/php_laravel_angular:latest
