name: Build and Push Docker Image

on:
  push:
    branches: [main]
   
  pull_request:
    branches: [main]
  

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v1
        with:
          username: saadbarhrouj
          password: dckr_pat_4729yU5bCn_BE3zx_inOjtD0ams

      - name: Build and push Docker image
        env:
          REPOSITORY: saadbarhrouj/php_laravel_angular
          IMAGE_TAG: SE
        run: |
          docker build -t saadbarhrouj/php_laravel_angular:SE ./configuration
          docker push  saadbarhrouj/php_laravel_angular:SE
